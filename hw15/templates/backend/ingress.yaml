{{- if and .Values.backend.enabled .Values.backend.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "backend.deployment.name" . }}-ingress
  labels:
    app: {{ .Values.backend.name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
spec:
  ingressClassName: {{ .Values.backend.ingress.className }}
  rules:
  - host: {{ .Values.backend.ingress.host }}
    http:
      paths:
      - path: {{ .Values.backend.ingress.path }}
        pathType: {{ .Values.backend.ingress.pathType }}
        backend:
          service:
            name: {{ include "backend.service.name" . }}
            port:
              number: {{ .Values.backend.service.port }}
{{- end }}